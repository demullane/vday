<p>THIS IS A SECRET PAGE THAT ONLY LOGGED IN USERS CAN VIEW</p>

<%= form_tag "/secret_page", {:method => 'get', :class => "form-horizontal"} do %>
    <div class = "form-group">
      <%= label_tag :tw, "Search for user:", class: "control-label col-md-2" %>
      <div class = "form-controls col-md-4">
        <%= text_field_tag :tw, nil, class: "form-control" %>
      </div>
    </div>
    <div class = "form-group">
      <div class = "col-md-offset-2 col-md-4">
        <%= submit_tag "Search", class: "btn btn-primary" %>
      </div>
    </div>
<% end %>

<hr>

<% @danielles.each do |tweet| %>
<%= tweet.text %><br />
<% end %>

<hr>

<% @found_tweets.each do |tweet| %>
<%= tweet.text %><br />
<% end %>
